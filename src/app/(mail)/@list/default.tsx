import { ThreadList } from "@/components/mail/ThreadList";

// Rendered when a full page load lands on a route the list has no page for.
export default function DefaultList() {
  return <ThreadList />;
}
